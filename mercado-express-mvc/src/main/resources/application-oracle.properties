# ===================================================================
# Profile ORACLE FIAP - Mercado Express MVC
# Para ativar: -Dspring.profiles.active=oracle
# ===================================================================
spring.datasource.url=${ORACLE_URL:jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL}
spring.datasource.username=${ORACLE_USER:RM562795}
spring.datasource.password=${ORACLE_PASSWORD:sua_senha_aqui}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
