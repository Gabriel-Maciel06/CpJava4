version: '3.8'

services:
  mercado-api:
    build: .
    container_name: mercado-express-api
    ports:
      - "8082:8082"
    environment:
      - SERVER_PORT=8082
      - SPRING_PROFILES_ACTIVE=prod
    restart: always
